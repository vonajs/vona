import { Bean } from 'vona';
import { LocalFlowFlowMessage } from './local.flow.flow/local.flow.flow_message.js';

@Bean({ scene: 'local.flow' })
export class ServiceFlowFlow extends LocalFlowFlowMessage {}
