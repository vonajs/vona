// eslint-disable-next-line
module.exports = app => {
  const config = {};

  // theme
  config.theme = {
    _theme: {
      name: 'cms-themeblog',
      url: 'https://www.npmjs.com/package/cabloy-module-api-cms-themeblog',
    },
  };

  return config;
};
