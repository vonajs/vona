// eslint-disable-next-line
module.exports = app => {
  const config = {};

  // plugin
  config.plugin = {
    track: {
      google: '',
      baidu: '',
      qq: '',
    },
  };

  return config;
};
