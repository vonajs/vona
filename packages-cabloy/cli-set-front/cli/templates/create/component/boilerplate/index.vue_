<template>
  <template></template>
</template>

<script setup lang="ts">
import { useMother } from '@cabloy/front-core';
import { Mother<%=argv.pageNameShortCapitalize%>, Props, Emits } from './mother.js';
const props = withDefaults(defineProps<Partial<Props>>(), Mother<%=argv.pageNameShortCapitalize%>.$propsDefault);
const emit = defineEmits<Emits>();
useMother(Mother<%=argv.pageNameShortCapitalize%>, props, emit);
</script>
